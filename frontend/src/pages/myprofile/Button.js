import Spinner from 'react-bootstrap/Spinner';
import './Button.css';

export default function MyButton(){
    return (
        <div className='divspinner'><Spinner className='spinner'/></div>
    );
}